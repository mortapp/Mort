name: MORT Feature Formatter

on:
  push:
    branches: [feature/mort-verify-age-school]
    paths:
      - flutter_mort/lib/features/trust/teen_verification_screens.dart
      - .github/workflows/mort-format-feature.yml

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feature/mort-verify-age-school
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.47.2
          cache: true
      - name: Resolve Flutter package configuration
        working-directory: flutter_mort
        run: flutter pub get
      - name: Format MORT Verify screen
        working-directory: flutter_mort
        run: dart format lib/features/trust/teen_verification_screens.dart
      - name: Commit formatter output
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No formatter changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add flutter_mort/lib/features/trust/teen_verification_screens.dart
          git commit -m "style(verify): format teen verification screen"
          git push origin HEAD:feature/mort-verify-age-school
